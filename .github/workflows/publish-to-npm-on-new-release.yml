name: Publish (NPM & GPR)

on:
  release:
    types: [created]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    defaults:
      run:
        working-directory: ./packages/nextjs-themes
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          registry-url: https://registry.npmjs.org
      - run: npm i -g pnpm && pnpm i
        name: Install dependencies
      - name: Test
        run: pnpm turbo build --filter persistnsync && npm test
      - name: Setup Git
        run: |
          git config --global user.name "mayank1513"
          git config --global user.email "mayank-automate@noreply.github.com"
          git fetch
          git checkout main
      - name: update version and push back to the repo
        run: pnpm changeset version && git push origin main
        working-directory: .

      - name: Publish persistNsync to NPM
        id: persistNsync
        working-directory: ./packages/persistnsync
        continue-on-error: true
        run: pnpm build && pnpm publish-package && pnpm publish-package2 # build again to include version update
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_AUTH_TOKEN}}
      - uses: actions/setup-node@v3
        if: steps.persistNsync.outcome == 'success'
        with:
          registry-url: https://npm.pkg.github.com/
      - name: Publish to GitHub Public Repository
        if: steps.persistNsync.outcome == 'success'
        working-directory: ./packages/persistnsync
        run: pnpm publish-gpr && pnpm publish-package2
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
      - uses: actions/setup-node@v3
        if: steps.persistNsync.outcome == 'success'
        with:
          registry-url: https://registry.npmjs.org

      - name: Publish Zustand-Sync to NPM
        id: syncZustand
        working-directory: ./packages/zustand-sync
        continue-on-error: true
        run: pnpm build && pnpm publish-package && pnpm pp2 && pnpm pp3 && pnpm pp4
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_AUTH_TOKEN}}
      - uses: actions/setup-node@v3
        if: steps.syncZustand.outcome == 'success'
        with:
          registry-url: https://npm.pkg.github.com/
      - name: Publish to GitHub Public Repository
        if: steps.syncZustand.outcome == 'success'
        working-directory: ./packages/zustand-sync
        run: pnpm p-gpr && pnpm pp2 && pnpm pp3 && pnpm pp4
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
      - uses: actions/setup-node@v3
        if: steps.syncZustand.outcome == 'success'
        with:
          registry-url: https://registry.npmjs.org

      - name: Publish nextjs-themes to NPM
        run: pnpm build && pnpm publish-package && pnpm publish-package2
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_AUTH_TOKEN}}

      - uses: actions/setup-node@v3
        with:
          registry-url: https://npm.pkg.github.com/
      - name: Publish to GitHub Public Repository
        run: pnpm publish-gpr && pnpm publish-gpr2
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}

      # Publish lite versions
      - run: node lite.js
      - uses: actions/setup-node@v3
        with:
          registry-url: https://registry.npmjs.org
      - name: Publish to npm
        run: pnpm build && pnpm publish-package && pnpm publish-package2
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_AUTH_TOKEN}}

      - uses: actions/setup-node@v3
        with:
          registry-url: https://npm.pkg.github.com/
      - name: Publish to GitHub Public Repository
        run: pnpm publish-gpr && pnpm publish-gpr2
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
